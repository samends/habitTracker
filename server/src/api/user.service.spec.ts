import {MockConnection} from '../utils/connection.stub';

const mockRepository = {
    find: () => {
        return {test: 'test'};
    },
};

describe('userService', () => {
    describe('when a user is being created', () => {
        beforeEach(() => {
            
        })
    })
})