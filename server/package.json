{
  "name": "habit-tracker-server",
  "version": "1.0.0",
  "description": "Server for habit tracker app",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm run lint",
    "clean": "rimraf .dist",
    "lint": "tslint -c tslint.json",
    "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sam Menza",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "express-graphql": "^0.7.1",
    "graphql": "^14.0.2",
    "pg": "^7.8.0"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "rimraf": "^2.6.3",
    "ts-node": "^7.0.1",
    "tslint": "^5.12.1",
    "typescript": "^3.2.4",
    "typescript-eslint-parser": "^22.0.0"
  }
}
